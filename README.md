# Axel Mendoza Website

## Installation
```bash
sudo apt-get install hugo
git submodule update --init --recursive
```

## Usage
Enable live server:
```bash
hugo server
```

Generate static page:
```bash
hugo
```